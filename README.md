# For this project, you need matplotlib.pyplot, tqdm and numpy. 
# These packages can be installed by:

pip install -r requirements.txt

# For checking the style run the file with

flake8 --config flake8.config $File
